target-version = "py311"
line-length = 120
src = ["app.py", "src", "tests"]

[lint]
select = ["E", "F", "W"]
ignore = [
  "E501", # line length handled separately
  "E402", # dotenv / OpenCV env must load before some imports
]
